metadata:
  version: "2.0"

deployments:
  - name: HF CommaCarSegments Mirror
    application: hf-comma-dataset-mirror
    deploymentType: Job
    version: latest
    resources:
      limits:
        cpu: 500
        memory: 1000
      replicas: 1
