FROM python:3.13-slim-trixie
ENV DEBIAN_FRONTEND=noninteractive \
    PYTHONUNBUFFERED=1 \
    PYTHONIOENCODING=UTF-8 \
    PYTHONPATH="/app"
ARG MAINAPPPATH=.
WORKDIR /app
COPY "${MAINAPPPATH}/plant/requirements.txt" "${MAINAPPPATH}/plant/requirements.txt"
RUN pip install --no-cache-dir -r "${MAINAPPPATH}/plant/requirements.txt"
COPY . .
WORKDIR "/app/${MAINAPPPATH}/plant"
ENTRYPOINT ["python3", "main.py"]
