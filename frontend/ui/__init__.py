"""Shared frontend helpers: navigation, selection state, error rendering.

Kept out of the page modules so the five pages contain page logic only and every
cross-cutting rule (the version selector, the blob-unavailable message, the
``pass_criteria`` table) exists exactly once.
"""
