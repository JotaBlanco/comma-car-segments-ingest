"use client"

import { StagePlaceholder } from "@/components/v-model/stage-placeholder"

export default function TestResultsPage() {
  return (
    <StagePlaceholder
      title="Test Results"
      description="The test results explorer and coverage matrix land in Phase 4. Requirements are available now."
    />
  )
}
